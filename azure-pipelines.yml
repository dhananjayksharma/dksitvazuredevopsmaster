trigger:
 - main

pool:
  vmImage: 'Ubuntu 20.04'

jobs:
  - job: job1
    steps:
      - script: echo Hello, World new changed!
        displayName: 'Run a one-line script'

      - script: |
          echo Add task to buid
          echo Add task to test 
          echo Add task to deploy
        displayName: 'Run a multi-line script'

    

